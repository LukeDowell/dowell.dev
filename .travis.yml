install: skip
language: java
jdk: openjdk11
env:
  - RELEASE_TAG="release-$TRAVIS_BUILD_NUMBER"
  - DOCKER_IMAGE_NAME="lukedowell/dowell.dev:$RELEASE_TAG"
services:
  - docker
script:
  - ./gradlew clean build
deploy:
  - provider: script
    script: bash scripts/build-and-deploy-docker-image.sh
